//arrow funcs con parametros, se pone a constante y luego el nombre y después de los parámetros las flechitas
const myConcat =(arr1, arr2)=> {
  return arr1.concat(arr2);
};

console.log(myConcat([1, 2], [3, 4, 5]));